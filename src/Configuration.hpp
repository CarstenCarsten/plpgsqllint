#ifndef CONFIGURATION_HPP
#define CONFIGURATION_HPP

#include <string>

using namespace std;

class Configuration {
        public:
                Configuration();
                string getInputFile(string filename);
};

#endif
