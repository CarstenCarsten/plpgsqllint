#ifndef LOGGER_HPP
#define LOGGER_HPP

#include <string>
#include <vector>

class Logger {
        private:
        protected:
        public:
                void error();
                void warn();
                void debug();
};

#endif
